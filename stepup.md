# stepup.md — the "everything bespoke" list

Full-site audit, 2026-07-27. Goal: every element considered; a visiting designer thinks "wow, he tastefully made this full of delight."

---

## The foundation — one hand, one vocabulary

- [ ] **Motion tokens.** ~37 distinct durations and 5 cubic-beziers in custom.css; ~95% of transitions are plain `ease`. Define `--ease-out-expo: cubic-bezier(0.16,1,0.3,1)` (the best moments already use it — founder portrait, studio reel), a spring, and 2–3 named durations, then sweep. Kill the one Material `(0.4,0,0.2,1)` curve on mobile pagers.
- [~] **Color tokens.** Five purples (#9F50FF/#9647F0/#8B1FFF/#8A3AEE/#7a2ff2), five pinks, three near-blacks on the studio nav alone — and three different "availability greens" (#AAFF00 home, #6BBF00 projects, #9EED3D 404, plus colophon's #b6ff2e). One signal green everywhere, or deliberately grade them like the cognac vintages and say so in the colophon. — GREENS DONE 2026-07-30; purples / pinks / near-blacks still open.
  - `--signal-green: #AAFF00` + `--signal-ring` now live at the top of custom.css with the full rationale and a measured contrast table. All four old greens retired (#9EED3D, #b6ff2e, #6BBF00 gone from code — remaining hits are comments only). Swept: ::selection, home/studio/projects avail-dots, 404 nav + btn + canvas, colophon links/code/caption/spec-hover AND its canvas rgba triples, cognak-global.js TEMP_PILL_COLORS.
  - **THE LIGHT-SURFACE RULE** (Pierce: never a dark green): the acid green is for DARK GROUNDS ONLY — #AAFF00 vs #EAE5F2 is 1.00:1, near-identical luminance, invisible. 3:1 there would need luminance <=0.23, i.e. a forest green. Instead of a second green, **light pages use their own accent for the same job**: the projects availability dot is now #4A3F6B (that page's dark lavender, matching its headline accent, 7.67:1). #6BBF00 retired. Never darken the green for light, never use it as text on light.
  - Contrast audit of green AS TEXT, measured: 404 CTA hover 4.07:1 PASSES (22–58px bold = large text, needs 3:1); all colophon green text 8.61:1 on the klein blue, passes. The one FAIL is the 404 nav link hover at 3.20:1 — but that's the mid-gray page, not the green: the nav's resting #EEEEEE on the bar's blended #808080 is already ~3.5:1. Fixing it means committing the 404 background (see the open item below), not changing the green.
  - Stale duplicates NOT swept: colophon1/2/3.astro still use #b6ff2e. They look like abandoned variants — candidates for the dead-code sweep.
- [x] **Per-page `::selection`.** — done 2026-07-30.
  - Added `data-page={page}` to `<body>` in BaseLayout as the CSS hook (only `window.COGNAK_PAGE` existed before). Nine page values covered; home + 404 keep the acid green (on 404 that IS the joke).
  - Pairs are MEASURED, not eyeballed — colophon green/klein-blue 8.61, privacy plum/pink 9.32, projects purple/white 4.71, studio pink/charcoal 10.97, brief orange/cream 6.71, send+receive blue/near-black 10.45, tools purple/charcoal 6.29. Keep any new page above 4.5:1.
  - The block is deliberately NOT DRY: `::-moz-selection` cannot be grouped with `::selection` in one selector list — an unknown pseudo-element invalidates the WHOLE list in every browser, silently killing both rules.
  - Brief's colours are hardcoded, NOT `var(--orange)`/`var(--cream)` — those tokens are scoped to `.brief` and don't resolve at body level.
  - CORRECTION to the audit's framing, verified live: privacy was a genuine clash (fluorescent green on a rose print-conceit page). **Colophon was NOT** — the green background was already that page's own accent (its links are green); the actual problem was the BLACK text, a value that appears nowhere on that page. The fix there only inverts the page's own two colours. Don't repeat "clashes on both" without looking.
- [x] **A scrollbar decision.** — done 2026-07-30. `color-scheme` + `scrollbar-color` + `scrollbar-width:thin` emitted on `html` from BaseLayout.
  - Light/dark is DERIVED from each page's own background luminance (threshold 0.25), not a hardcoded list, so a new page can't drift out of sync with its own colour.
  - TWO PAGES DON'T PASS `pageBg` and need explicit fallbacks in `PAGE_BG_FALLBACK`: home paints #17151a from CSS, project detail resolves to WHITE. "No pageBg = light" would put a light scrollbar on the darkest page on the site.
  - Thumb = the page's ::selection accent at 60% alpha (8-digit hex), so it tints rather than stripes. Verified all 11 pages classify correctly.
  - `color-scheme` also themes NATIVE FORM CONTROLS — deliberate. Visible ones: /brief range slider, /tools number spinners, /send autofill. Most other inputs are opacity:0 behind custom UI.
  - Safari deliberately left alone: no `scrollbar-color` support, and `::-webkit-scrollbar` would replace macOS overlay scrollbars with always-visible ones — worse than the default being fixed.
- [ ] **Body-link underline system.** Privacy already has the proto (offset underline, color fade) — promote to a site-wide animated underline draw so inline text links have a signature move distinct from nav links.
- [ ] **Delete the second design system.** screen.css ships ~800 lines of WordPress-era rules: 0.7s `all` transitions (52 of them), Facebook-blue socicon hovers, ninja-forms, hamburger.
- [ ] **Dead code sweep** (inspectors will read the source): `letter-rainbow-in` (studio, unused), `.colophon-eyebrow` (styled + JS-queried, element doesn't exist), `.copied` + old `.coloredit` block (brief), `.footer-backtotop:hover` whose hover color equals its resting color (no-op hover), and the fully-built-but-never-fed `is-video` cursor path.

## Homepage

- [ ] **Hover-meta exit typing.** On mouseleave, backspace the JSON out at 2× speed instead of letting stale meta linger. Keeps the terminal fiction coherent.
- [ ] **Wire the dead video cursor.** Emit `data-hero-video` on cards so the 140px cursor panel plays the reel for video projects (DuVine has a hoverVideo ready). The CSS and JS branch already exist.
- [ ] **"+ more" end-state morph.** After revealing hidden tiles, the second click silently navigates to /projects. Rotate the + 45° and morph the label to "all projects →" so the changed meaning is honest.
- [ ] **Weather-reactive dust motes.** `fetchWeather()` already knows LA conditions — on rain codes, let the motes fall like drizzle. Zero extra network.
- [ ] **Odd-count grid hole.** Featured grid is 2-col with no odd-count handling (the archive grid solved this with `direction: rtl`).

## /projects

- [x] **Give the default view the morph.** Only grid figures carry `view-transition-name`, so list users (the default) get a plain fade into the detail page while grid users get the signature thumb→hero morph. Give list rows a name too. — done 2026-07-30, verified live in Chrome 150.
  - `.projects-list-item` now carries `view-transition-name: project-thumb-{slug}`. Safe because grid and list are never both visible (`display:none` swaps them), so the shared names can't collide.
  - Styling the pseudo-elements without enumerating 53 dynamic names: `view-transition-class: proj-thumb` on the grid figure, the list row AND `.img-wide--hero`, then one rule on `::view-transition-old(.proj-thumb)`. Gated behind a new `list-morph` transition type so the grid path is untouched — the old snapshot of a text row visibly stretches while the box grows, so it's dropped in 90ms; image→image keeps the full cross-fade.
  - The type is added in BaseLayout's `pagereveal`, gated on `localStorage.cognak-projects-view`, NOT on a click flag. For a cross-document transition the `::view-transition-*` pseudo-elements live in the DESTINATION document, so a flag set on mousedown in /projects never arrives. localStorage is the only same-origin signal that survives the navigation.
  - **FOUND TWO PRE-EXISTING BUGS IN vt-isolate** (grid-only version had both; they only bite from the SECOND click onward, so this read as "the morph stopped working", and the grid morph was quietly degraded too). (1) Nothing restored the names after a COMPLETED navigation — the mouseup handler only restored on an aborted click, and Chrome serves the archive back from bfcache still zeroed, so the next click's "keep" element is already `none` and there's no participant to pair with the hero. (2) The restore read a stash of the PREVIOUS value (`dataset.savedVt`), which on the second pass was itself `none` — the real name was overwritten and permanently destroyed, so restoring wrote `none` over `none`. Measured live: 53/53 rows at `none`, 53/53 stashes reading `none`. Fix: snapshot the canonical names ONCE at init into a WeakMap (ground truth that nothing can clobber) + restore on `pageshow`. pagereveal runs before pageshow, so restoring can't disturb a back-navigation's own morph.
  - DEBUGGING NOTE for next time: extension-driven synthetic clicks do NOT get a cross-document view transition — `pageswap` fires with `e.viewTransition === null`. Automated clicking cannot test this; a real human click is required. Instrument by writing `{hasVT, types, keptNames}` to sessionStorage from a `pageswap` listener, then read it on the destination page.
- [x] **FLIP the sort.** The clock/abc SMIL icons are jewels, then the reorder is an instant `appendChild` snap. Record rects → reorder → play with the expo curve. — done 2026-07-30. (list⇄grid deliberately NOT done — split off, see below.)
  - Hand-rolled FLIP in `cognak-projects.js`, 520ms on the expo curve. **NOT `document.startViewTransition`**, and this is the important note: the tiles and rows carry PERMANENT `view-transition-name`s for the navigation morph, so a same-document transition would snapshot all 53 at once (what vt-isolate exists to prevent), AND the root rules are navigation-tuned — `::view-transition-old(root){animation:none;opacity:0}` would blank the outgoing page. Any future same-document VT on this page hits the same wall.
  - Stagger is keyed to TRAVEL DISTANCE, not index (0–120ms spread, furthest starts last). Index-keyed reads as a wave sweeping down the page and says nothing about what changed; distance-keyed reads as the list settling into its new order.
  - Only items within 200px of the viewport animate — 53 rows all animating stutters, and the cheaters are off-screen by definition. Also `animate=false` on the on-load call: the server already renders newest-first, so that pass is a no-op sort, and skipping it avoids 53 `getBoundingClientRect()` reads during first paint.
  - **`--ease-out-expo: cubic-bezier(0.16,1,0.3,1)` now exists** at the top of custom.css with usage guidance (use for things that MOVE; keep plain `ease` for small color/opacity fades). First customer is this FLIP. That's the motion-token foundation item started — the full sweep of ~37 durations is still open.
  - KNOWN, NOT ADDRESSED: `initLazyLoad()` runs after every sort and reapplies `.proj-lazy` (opacity 0) to items past index 8, so in GRID view a tile sliding into view can blink before the IntersectionObserver re-shows it. Pre-existing; the slide may make it more visible.
- [ ] **FLIP the list⇄grid toggle.** Split out of the sort item. Needs a hand-rolled crossfade+rise for the same reason the sort did — `document.startViewTransition` is off the table on this page.
- [~] **Grid-view hover is dead** (transform explicitly nulled, no media swap). — SKIPPED by Pierce 2026-07-30. Two findings worth keeping if it's ever revisited: (a) the audit's "borrow the homepage's hover-media swap" is WRONG for this page — `cognak-global.js` already loads the hovered project's hero (and heroVideo) into the 140px cursor panel for both `.project-excerpt` and `.projects-list-item`, so a tile media swap would put the same image on screen twice, inches apart; (b) the only sensible move left was porting the homepage's lerp tilt (`perspective(800px)` rotateY/rotateX, lerp 0.07) at ±2° instead of ±3° — but note these tiles carry `view-transition-name`, and transforming a named element can distort the morph's captured geometry (the homepage gets away with it at ~6 tiles; untested at 53).
- [~] **Mobile list is bare text rows** — tags hidden, no cursor, no imagery. — SKIPPED by Pierce 2026-07-30: the cask grades aren't important enough to earn space on mobile. Thumbnails also declined. Treat mobile's bare-title list as INTENTIONAL from here — don't re-raise it. (Mechanics if it ever changes: `.list-item-tag {display:none!important}` at <=720px hides category AND grade together; showing the grade alone would need the "·" separator moved out of the vintage text into a CSS ::before, since it's currently baked into both the markup and the live-refresh JS.)

## Project detail (audited via DuVine)

- [ ] **The gallery has no design.** DuVine's five gallery images sit inside a single `<p>`, stacking edge-to-edge with no spacing, no reveal, no lazy fade. Fix: IO-gated fade + 12px rise (the existing `hp-lazy` spec), spacing rhythm between images, stagger when two enter together.
- [ ] **Alt text → captions.** DuVine's alts are already written like captions ("— interior map", "— magazine hero"). Render as optional mono micro-captions that fade in after the image settles. Zero new authoring cost.
- [ ] **Entrance choreography.** The 70px mono title, category, and three detail columns appear statically — the only page with no entrance at all. Word-stagger the title, accent-fade the category, rise the columns 80ms apart.
- [ ] **Pay off the vintage.** DuVine is a 2013-dated, 10-year relationship — literally an XO. The cask grade appears in the archive list and homepage hover-meta but never on the detail page. Small mono chip beside the category.
- [ ] **Directional prev/next.** The `proj-switch` crossfade is flat; add a 24px slide keyed to direction under the existing 220ms. Wire the built-but-unused `is-next` spinning-ring cursor to the side pagers.
- [ ] **End-of-gallery pager peek.** When the last image enters view, slide both pagers fully in for ~1.2s then re-park — a designed "you've reached the end, next?" beat.
- [ ] **Content nits.** DuVine's `projectType` has a trailing comma (`…Development, '`), `projectYear: 2016-2026` uses a hyphen not an en-dash, and the body still carries `wp-image-2668` classes. Worth a content-collection lint pass.

## /studio

- [x] **Give the reel a sense of place.** All 15 POOL entries have empty client/kind/year — the typewriter only prints the random fields, and `revisions`/`nda` re-randomize per view, breaking the "record" illusion. Fill the metadata, seed the fake fields from the asset src so they're stable, add `"asset": "03 / 15"` as the first typed line, and a blinking caret that settles after typing.
  - Done 2026-07-30. All 15 POOL entries filled. The 3 unlabelled assets were identified from the images themselves: strawberry.png = Snow Monkey, zynlo-mockup.jpg = ZYNLO Bank, 1920x1080-Refresh_1.mp4 = DuVine ("Refresh your perception" cycling film). ZYNLO deliberately has NO year — its /media/2015/12/ folder is a WordPress-import catch-all (CTP sits there too) and the bank didn't exist in 2015; buildMeta skips empty fields.
  - render_id / revisions / nda were `Math.random()` PER RENDER, so the same asset reported a different record every time you cycled back to it. Now seeded from an FNV-1a hash of `item.src` — fixed identity per asset, survives reloads, no hand-authoring.
  - `"asset": "08 / 15"` leads the record; caret is solid while typing (blinking mid-keystroke reads as a stall), blinks after, fades at 2s via .is-typing / .is-done on .sfv-meta.
  - BUG FIXED IN PASSING: mobile `.sfv-meta` is a FIXED height with `overflow:hidden`, sized for the old 8-line record. The new asset line makes 9 and would have silently clipped `"nda"` on phones. Raised 142 -> 160px. Any future field needs the same bump — it fails silently.
- [x] **Tabs are the flattest interaction on the page.** Bare 0.35s opacity swap. A small pink marker gliding between Approach/Founder + a 6–8px panel rise on the expo curve — and `role="tablist"`/arrow keys while in there.
  - Done 2026-07-30. NOTE the audit's "gliding between" implies a horizontal underline — `.studio-tabs` is `flex-direction:column`, so the marker travels VERTICALLY.
  - 1px pink rule (Pierce's call; matches the homepage `.scroll-discover-track` exactly — same width, hue, and job). Gutter via `padding-left:20px; margin-left:-20px` = net zero layout shift, keeping tab labels aligned with the panel copy; the page margin is only ~35px on phones and would have clipped it.
  - MOTION, after a revision: v1 snapped to spanning both tabs, held 170ms, contracted — Pierce read that as a bounce. Now the two EDGES animate independently (`top` + `bottom`, not position + height): leading edge 340ms, trailing 620ms, swapped by direction. The stretch is a consequence of the lag rather than a phase, so there's nothing to snap back from. Both edges stay on expo (no overshoot) — if it ever needs to feel springier, lengthen TRAIL_MS; do NOT reach for an elastic curve, that's the bounce again.
  - `bot` = `tabList.clientHeight - (top + height)`. clientHeight is the padding box and .studio-tabs has no vertical padding, so it shares offsetTop's frame. Adding vertical padding there breaks this.
  - Resize and fonts.ready re-measure with BOTH edges landing together, and the first placement uses 0s so the rule doesn't sweep in from the top of the list on load.
  - Panels: 7px rise on expo, opacity 0.35 -> 0.42s. Full tab pattern — role=tablist/tab/tabpanel, aria-orientation=vertical, aria-selected, roving tabindex, arrow keys + Home/End. `<nav>` became `<div>`: a tablist isn't site navigation.
- [x] **The manifesto has no entrance** — the thesis sentence is the only prominent text missing the site's stagger. Add it, and let the three pink accent words land last as a three-beat cadence.
  - Done 2026-07-30, but NOT via cognak-global.js's SELECTORS. **KEY DISCOVERY: the global splitWords only walks TEXT nodes** — element children are re-appended whole and never get `.stagger-word`. That is the real reason every other page carries a separate `headline-accent-fade` rule for its `<em>`/accent spans. Here it would have been fatal: the three pink words would have been the ONLY ones already visible, landing FIRST instead of last.
  - So the manifesto has a local entrance in studio.astro reusing the identical spec (`.stagger-word`/`.stagger-in`, 28ms cadence, same IntersectionObserver trigger) but ordering by ROLE, not DOM position. Sentence assembles around three holes, 120ms pause, then accents 130ms apart. `.stagger-word` is inline-block at opacity 0, so the holes hold their space — no reflow.
  - `html.js .studio-manifesto{opacity:0}` added to BOTH the hide list and the reduced-motion escape list (the /send July 2026 invisible-headline bug). Extra important here because the manifesto ISN'T in the global list, so nothing else would ever set it back.
- [x] **"Ta-da!" is a dead end.** The award badges pop delightfully then can't be clicked (`pointer-events:none`). Re-enable on desktop with a 150ms delay after activation.
  - Done 2026-07-30. **The audit's fix alone would NOT have worked.** `.award-badges-wrap` is at `left: calc(100% + 20px)` — far right of the subhead column, nowhere near the word that triggers it — and the badges hide on the word's `mouseleave`. Enabling pointer-events alone gives you targets that vanish the instant you move toward them.
  - Needed BOTH: `ARM_DELAY` 150ms (the audit's part, so a click already in flight can't land on a badge that just appeared under the cursor) AND `HIDE_GRACE` 260ms on mouseleave, cancelled when the pointer enters the wrap — so the badges survive the journey and stay up while hovered.
  - The `pointer-events:none` was deliberate, not an oversight (comment cites iOS scroll-jump + accidental navigation). Re-enabled ONLY under `(hover:hover) and (pointer:fine)` and only with `.is-armed`; the mobile block still forces `pointer-events:none !important` on the wrap.
  - The pill drops on the word's mouseleave IMMEDIATELY, not on the delayed hide — the global cursor handler early-returns while `is-founder` is set, so holding it would freeze the cursor for the whole grace period. Hovering a badge re-adds the pill reading "View on CSSDA ↗": there's no OS cursor on this site, so the pill is the only thing that can carry the affordance.
  - a11y: the three links keep `tabindex="-1"`, so they're mouse-only. SKIPPED by Pierce 2026-07-30 — don't re-raise. (Fixing it would mean making the mid-sentence `.award-name` span focusable and revealing badges on focus, which changes the reading flow of a paragraph. The badges are an easter egg, not a primary route to the CSSDA entry.)
- [x] **Logo ring ↔ scroll.** Feed Lenis velocity into the ring's target speed (clamped) so fast scrolling visibly spins it before it settles back. Also: the ring ignores reduced-motion (the only animation on the page that does).
  - Done 2026-07-30. `window._lenis.velocity` -> clamped boost on top of the base speed; the existing 0.055 lerp does the settle. Measured: 83.8s/rev resting -> 7.6s/rev at the clamp (~11x). Sign preserved, so scrolling UP briefly reverses the ring — one `Math.abs()` away from forward-only if that ever reads wrong. Knobs: BOOST_MAX (10x resting), VEL_REF (60 ≈ a brisk flick).
  - REDUCED MOTION — the naive fix breaks the page: the 15 logos are `position:fixed` and placed ENTIRELY by this RAF loop, so stopping the loop stacks them all at 0,0. Instead the geometry is kept and only the movement dropped: speed pinned to 0, RAF stops self-scheduling after one pass, and a redraw is bound to scroll/resize (measureStatic/updateCenterFromScroll only recompute cx/cy — they don't paint). Fade-in skipped too. Bonus: Lenis itself doesn't init under reduced motion, so `_lenis` is undefined and the velocity coupling is inert by construction.
- [~] **The dark→light footer seam** is the page's only hard cut — a short fade zone or hairline treatment above `.main-footer`. — SKIPPED by Pierce 2026-07-30. (For reference if revisited: /studio is #18151A and `.main-footer` is `background:#f4f3f3` from screen.css:554 — a 1px near-black to near-white cut. Any treatment must be scoped to the DARK pages; the same footer sits under /projects #EAE5F2 and /privacy #F1D6EA where it's barely a transition.)
- [x] **Avail-dot conflict.** custom.css says the studio avail-dot is pink #FFB2E4; inline styles force acid green. One of them is wrong on purpose — make it look that way.
  - Done 2026-07-30 — PINK WINS. It was contradicting itself in THREE places, not two: custom.css:2512 pink, a `!important` rule in studio.astro, and an inline style on the span. Both green overrides removed; custom.css's stated intent now stands unopposed. /studio is now green-free entirely.
  - This generalised the colour rule, and the signal-green header comment was updated to say so: **the green is the DEFAULT, not the mandate.** It holds the homepage and 404, and yields wherever a page has its own voice — dark or light. /projects yields to its purple (legibility forced it); /studio yields to pink even though the green was perfectly legible there, because a lone green dot on a page that speaks pink throughout reads as imported.

## /brief

> **ALL SIX DONE 2026-07-30, one pass.** Cross-cutting notes: the orange is now tokenized (`--orange` / `--orange-hi` / `--orange-rgb` / `--rail-cream`) in the `.brief` var block, and all new motion uses `--ease-out-expo` from custom.css. `updateProgress()` still interpolates cream->orange NUMERICALLY in JS (hardcoded 237,224,200 -> 239,145,70) — if the token changes, that math must change too. Every new animation has a reduced-motion off-switch.

- [x] **The Sent screen is a hard `display` swap** — the emotional payoff of the entire funnel, and the only step with zero transition. Crossfade + staggered rise (eyebrow → headline words → lede → ghost button), sparks starting only after the headline lands. Biggest single win on the site.
  - Done: the form crossfades out via the existing `.step-hide` (same 260ms as every other step change), then Sent rises in parts. Headline is split word-by-word by a local `splitSentWords()` that WALKS CHILD NODES rather than touching innerHTML — so the `<em>Got it</em>` accent survives and keeps its orange. Named distinctly from cognak-global.js's `splitWords` (that one is IIFE-scoped and uses `.stagger-word`; this uses `.sw`).
  - Delays are handed to each element as a `--d` custom property that the CSS transition reads, rather than per-element JS transitions.
  - Sparks are HELD (`animation-play-state:paused`) until the last headline word lands, then armed with `.sparks-go`. Glitter before the payoff reads as decoration; glitter on "Got it" reads as a reaction.
  - TRAP FOUND: `#sent .spark{opacity:0}` out-specifies the earlier `.brief .spark` reduced-motion rule (id beats class), which would have left reduced-motion users with NO glitter at all. The reduced-motion block now restates `opacity:.85`.
  - `selectDiscipline()` clears `.sent-in` + `.sparks-go` so a second brief replays the entrance instead of appearing already-arrived.
- [x] **Submit button state machine.** "Sending…" is a bare text swap with no disabled styling. Arrow slides away → shimmer while sending → a 300ms "Sent ✓" beat before the screen swap → shake + red flash on failure.
  - Done: button markup split into `.btn-label` + `.btn-arrow` so the arrow can leave on its own. Arrow slides out -> `.is-sending` dims the fill and runs a shimmer sweep -> `.is-sent` shows "Sent ✓" for a 300ms beat BEFORE the screen swaps (without the beat, button and screen change in the same frame and the click is never acknowledged) -> `.is-failed` shakes and flashes red, self-clearing after 900ms.
  - Button state is reset after the swap so Back / "Start another brief" never finds it stuck.
- [x] **The form is listening — show it.** The budget follow-up (`.bplus`) pops via `hidden` toggle; give it the modal's rise. Chip checkmarks draw with no animation; scale-rotate them in. Pills snap; give selection a micro spring. Invalid fields get a restrained ±3px shake instead of an instant red border.
  - Done, all four: `.bplus` rises on the expo curve (pure CSS — the `hidden` attribute flips display, which replays the animation, no JS needed; safe because `.bplus` is a div, unlike the `.btn{display:inline-flex}` trap). Chip checkmarks scale-rotate in — NOTE every keyframe must restate `rotate(-45deg)` or the tick snaps upright mid-animation. Pills get a `pillPop` micro spring. Invalid fields get a ±3px `fieldShake` instead of only an instant red border.
- [x] **Progress rail completion moment.** Reaching 100% is silent — one glow swell and the end-cap tinting orange.
  - Done: at 100% the rail swells once and the bottom end-cap tints orange. Guarded on the TRANSITION to 100 via `lastPct` — `updateProgress()` runs on every keystroke, and a glow that re-swells whenever you touch an already-complete form is noise, not a reward. `lastPct` resets in `selectDiscipline()`.
- [x] **Resurrect `.stepmeta`.** Styled and included in the entrance stagger but never rendered. "02 / BRANDING — ~2 MIN LEFT" ties the picker's time promise to the rail.
  - Done: renders `01 / BRANDING — ~4 MIN LEFT`. The minutes come from `PICKER_META` (the picker's own promise) and are counted DOWN against real completion, which is the actual point of the item — it ties the picker's time promise to the rail so the rail stops being an abstract percentage. At 100% it stops estimating and reads `READY TO SEND`.
- [x] **Tokenize the orange.** `#EF9146` appears ~20 times raw, with two slightly different hover-oranges on sibling elements.

## /colophon

- [x] **Fix the body links first** (no hover, no transition — worst offender on the page for this audience), and give external credits a mono `↗`. — done 2026-07-30: resting underline at 45%, hover thickens 1→2px + tightens offset 3→2px + goes full green, 0.25s expo. Mono `↗` on prose external links only; spec block deliberately left arrow-free (5 of 6 rows are external) and keeps its color-shift hover.
- [x] **Glyph caption readout.** A tiny fixed mono caption near the canvas ("lowercase g · 1000 upm · 138 pts") updating as the morph cycles — specimen-plate labeling. The data's already in `DATA`. — done 2026-07-30: reads `03 / 07 · lowercase g · 1000 upm · 2 contours · h 721`. Contour count and height are MEASURED off the outline data at boot (degenerate padding contours filtered), not authored. Swaps at the morph midpoint with a sine dip to 0.15; hidden below 900px; anchored past the text column's right edge, not the glyph's left (layout() shifts the glyph −300px, so its left edge is inside the column).
- [x] **Cursor-aware anatomy.** Nodes/handles near the cursor brighten slightly — the artwork begs inspection and it's the only page canvas that ignores the pointer (privacy ripples, 404 flees). — done 2026-07-30: 190px smoothstep falloff off a lerped influence point (0.15/frame, so the light trails the cursor); alpha ×1→2.8 AND tangent handles 16→20px near the pointer. Tuning knobs are PROX_R / PROX_GAIN / the `16 + 4 * prox`. Breathing halo kept on purpose (pulse not damped) — damping `pulse` toward 1 is the one-liner if it reads busy. Armed only on (hover:hover) and non-reduced-motion.
- [x] **The ":)" payoff.** Tint the stroke green for just that glyph in the cycle — turns a hidden joke into a discovered one. — done 2026-07-30: outline lerps #f4f4ee → #b6ff2e by a `smile` amount read off BOTH morph endpoints, so it greens on the way in (k→smile) and drains on the way out (smile→c) instead of snapping.
- [x] **Print stylesheet.** A colophon is exactly the page a type nerd prints. Two dozen lines; huge signal. (Privacy deserves one too — it literally draws crop marks.) — done 2026-07-30 for colophon; privacy still owes one. NOTE the real bug it fixed: `.stagger-word` sits at opacity 0 until its IntersectionObserver fires, so anything below the fold printed BLANK. Any other page that gets a print stylesheet needs the same reveal guard.
- [x] **Date it.** Add a `Set` row to the spec grid with the build date — colophons traditionally date themselves. — done 2026-07-30: `buildDate` resolves in the Astro frontmatter at build time (en-GB, "30 July 2026"), so it names the deploy. Vercel builds in UTC — a late-night LA deploy dates as the next day.

## /privacy-policy

- [x] **Numbered mono section headers** (`01 · Analytics` in Diatype Mono) — the site's "label voice" is mono everywhere except here, and it feeds the page's print conceit. — done 2026-07-30: UPPERCASE (matches the existing rule + colophon spec labels), Diatype Mono 12px/0.12em, numbers via CSS counter on `.privacy-body` (add/reorder sections and they renumber themselves) at 55% opacity. In `custom.css`, not the page file.
- [x] **Cookie ledger + live consent readout.** Replace the prose cookie list with a hairline mono table, and add `your current choice: declined — change` that reads the cookie and re-fires the consent update. Genuinely useful and perfectly on-message. — done 2026-07-30. IMPORTANT context found while building: the BaseLayout banner hides itself forever once `cognak_consent` exists, so this readout is the ONLY way to change consent on the site. It therefore does two things the banner doesn't: sends an explicit DENIED `gtag consent update` (banner skips it since default is denied), and expires existing `_ga`/`_ga_*`/`_gcl_*` across domain variants on decline. Cookie name + 1-year expiry are now duplicated in BaseLayout AND privacy-policy — change both. Ledger is a grid of `<div>`s, never `<p>`/`<h2>`, to stay out of the word-stagger's opacity:0 trap.
- [x] **Deliver the headline's promise.** One-line mono glosses under each section — "in short: we count visits, not people." — done 2026-07-30: `.pp-gloss` under 01–05, mono 12px behind a hairline left rule with an "IN SHORT" ::before label. 06 Questions left bare on purpose (it's just an email address). Note the 04 gloss ("Ask, and we delete it.") drops the paragraph's "EU visitors" qualifier — deliberate, hedging the plain-English line defeats its purpose.
- [x] **Baseline-lock the type.** The canvas draws a baseline grid derived from the body line-height, but the text doesn't actually sit on it — designers will check. Quantize the margins and the whole conceit clicks. — done 2026-07-30, margins-only (option 1). Grid unit is 28.8px (16 x 1.8, read off `.privacy-body p` at runtime): p 20→28.8, h2 56/16→57.6/14.4, headline 56→57.6 (mobile 40→43.2), gloss/ledger/consent 20→28.8. ALSO phased the drawn rules onto the real baselines via canvas fontBoundingBox metrics + a `gridPhase`, re-measured on fonts.ready and load — aligning the decoration to the type instead of shoving the type onto an arbitrary origin. If you ever change that 1.8 line-height, every quantized margin needs rechecking.

## /404

- [ ] **Commit to a color.** Flat #6F6F6F is the least chosen-looking decision on the site. Go near-black graphite and let the green dot be the only saturated thing on the page — which is the joke: the availability dot is the only thing still working. — ATTEMPTED + REVERTED 2026-07-30, still open. Tried #242424 neutral graphite; Pierce didn't like it. Findings worth keeping for the next attempt: (a) any #14–#18 near-black is out — /tools #16151A, home #17151a, /studio #18151A are all violet charcoal, /send+/receive #090C0B, /brief #140E07, so it'd read as a repeat rather than a choice; (b) going LIGHT (#E7E7E4 was floated) forces the green to change — #AAFF00 measures 1.00:1 on it, i.e. identical luminance, invisible; #4F7A00 (4.12:1) was the workable substitute; (c) both nav bars use rgba(129,129,129,.95), a tint of the current gray — any bg change has to update those too, desktop AND mobile.
- [x] **Let the dot find the exits.** Occasionally weight its wander toward the two CTA links' rects. The lost dot quietly shows you the way out. Pure narrative, zero new UI. — done 2026-07-30: `EXIT_BIAS = 0.34`, so ~1 waypoint in 3 targets a CTA rect (biased just below the baseline so it passes under the words). Rects read at pick() time, not cached — stays correct across resize/scroll with no invalidation, and pick() runs a few times a minute, not per frame.
- [x] **The frown deserves a resolution.** After the 1.6s tilt, carry ":(" on through so it reads as a sideways smile — earning the "either way, we're glad you're here" line. — done 2026-07-30, but NOT as written: rotation alone can never turn a frown into a smile (it preserves the eyes↔mouth-curve relationship; ":(" at 180deg is just "):"). The mouth has to mirror. Glyph is now split into `.ff-eyes` + `.ff-mouth` spans; hover rotates the wrapper 90deg and `scaleX(-1)`s the mouth alone — eyes on top, mouth curving up. Mouth flip delayed 0.5s so it reads tip-then-smile.
- [x] **The CTAs are the barest element on the page** — the whole point of a 404, with color-only hover and no entrance. Arrow slide-in + staggered entrance after the body. — done 2026-07-30: reuses the HOMEPAGE arrow (same inline SVG as `.hp-projects-arrow`, points down at rest, rotates -90deg on hover) rather than a bespoke glyph, so the 404 makes the site's existing gesture; entrance staggers 620ms + 90ms apart. Reveal script is its OWN top-level IIFE with no early return (inside the canvas block it would strand the CTAs hidden for reduced-motion users). nowrap scoped to (hover:hover) only — unconditional nowrap on "Discover the studio" at 8vw is a mobile-overflow risk.
- [x] **Bugs while in there.** The headline/body are missing from the pre-hide FOUC guard (only staggered page exposed to flash), the accent misses `headline-accent-fade`, reduced-motion users get a completely empty gray page, and the canvas dot green ≠ the CSS hover green on the same page. — all four done 2026-07-30. (1) `.fourofour-headline` + `.fourofour-body` added to the html.js hide list AND its reduced-motion override — there's now a comment on that block saying both lists must change together. (2) `.fourofour-accent` joins headline-accent-fade. (3) Reduced motion no longer `return`s out of the canvas script; it draws one static dot parked near a CTA, so the page keeps its subject. Correction to the audit: text was never actually blank there — the stagger exits before splitting — what was missing was the dot. (4) nav pulse-dot + btn hover #9EED3D → #AAFF00 to match the canvas (also better on the gray: 4.07:1 vs 3.50:1).

---

**Suggested first batch:** motion/color tokens + per-page selection, the brief Sent screen, and the project gallery treatment.
